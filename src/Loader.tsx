import React from "react";

const Loader = () => (
  <div
    style={{
      width: "100%",
      textAlign: "center",
      alignItems: "center",
      justifyContent: "center",
      fontWeight: "bold",
      fontSize: ".7rem",
      color: "#aaa",
    }}
  >
    Loading ...
  </div>
);

export default Loader;
export { Loader };
